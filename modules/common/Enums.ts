export enum TelegramEventType {
    JOINED_CHAT = 0,
    LEFT_CHAT = 1
  }
  
  export enum BlockchainEventType {
    USER_REFERRED = 0,
    USER_RETAINED_TWO_WEEKS = 1,
    USER_RETAINED_ONE_MONTH = 2
  }
  